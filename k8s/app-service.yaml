apiVersion: v1
kind: Service
metadata:
  name: hire4j-app-service
spec:
  selector:
    app: hire4j-app 
  ports:
    - protocol: TCP
      port: 8080       
      targetPort: 8080 
  type: NodePort    
  
  
